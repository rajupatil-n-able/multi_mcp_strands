[project]
name = "multi-mcp-strands"
version = "0.1.0"
description = "Multi-agent Strands demo with multiple MCP servers"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.27.1",
    "strands-agents>=1.39.0",
    "python-dotenv>=1.0.0",
]
